{
  "name": "hush",
  "version": "0.6.0",
  "description": "Per-site element hiding, DOM removal, and network blocking for Chrome (MV3)",
  "private": true,
  "scripts": {
    "test": "node --test test/"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
